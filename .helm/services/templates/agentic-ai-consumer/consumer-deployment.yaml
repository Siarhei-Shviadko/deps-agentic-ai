apiVersion: apps/v1
kind: Deployment
metadata:
  name: agentic-ai-consumer
  labels:
    {{- include "deps-agentic-ai.labels" . | nindent 4 }}
    app.kubernetes.io/component: agentic-ai-consumer
spec:
  replicas: {{ .Values.agentic_ai_consumer.replicaCount }}
  selector:
    matchLabels:
      {{- include "deps-agentic-ai.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: agentic-ai-consumer
  template:
    metadata:
      annotations:
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "deps-agentic-ai.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: agentic-ai-consumer
    spec:
      {{- if .Values.agentic_ai_consumer.imagePullSecrets }}
      imagePullSecrets:
        - name: {{ .Values.agentic_ai_consumer.imagePullSecrets }}
      {{- end }}
      {{- if .Values.podSecurityContext.runAsNonRoot }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      {{- end }}
      {{- if .Values.vault_settings.enabled }}
      serviceAccountName: {{ .Values.service_account.serviceAccountName }}
      {{- end }}
      {{- if .Values.agentic_ai_settings.DATABASE_REQUIRE_SECURE_TRANSPORT }}
      initContainers:
        - name: init-{{ .Chart.Name }}
          image: busybox
          command: ["sh", "-c", "cp /home/inputdbcerts/* /home/outputdbcerts/ && chmod g-r /home/outputdbcerts/*.key"]
          volumeMounts:
            - name: tls-database-certs
              mountPath: /home/inputdbcerts
            - name: outputdbcerts
              mountPath: /home/outputdbcerts
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: '{{ .Values.registry }}{{ .Values.agentic_ai_consumer.image.path }}:{{ .Values.agentic_ai_consumer.image.tag | default "latest" }}'
          imagePullPolicy: {{ .Values.agentic_ai_consumer.image.pullPolicy }}
          command: ["python", "-m", "deps_agentic_ai", "dispatch"] # "consume" or "dispatch"?
          resources:
            {{- toYaml .Values.agentic_ai_consumer.resources | nindent 12 }}
          envFrom:
          - configMapRef:
              name: agentic-ai-configmap
              optional: false
          env:
          {{- if .Values.vault_settings.enabled }}
          {{- toYaml .Values.agentic_ai_consumer_vault_enabled.env | nindent 10 }}
          {{ if .Values.agentic_ai_settings.RABBITMQ_TLS_ENABLED }}
          - name: RABBITMQ_TLS_PASSWORD
            valueFrom:
              secretKeyRef:
                name: deps-agentic-ai
                key: rabbitmq-password
          {{ end }}
          {{ if .Values.agentic_ai_settings.KAFKA_SECURITY_ENABLED }}
          - name: KAFKA_SASL_USERNAME
            valueFrom:
              secretKeyRef:
                name: deps-agentic-ai
                key: kafka-sasl-username
          - name: KAFKA_SASL_PASSWORD
            valueFrom:
              secretKeyRef:
                name: deps-agentic-ai
                key: kafka-sasl-password
          {{ end }}
          {{- else }}
          {{- toYaml .Values.agentic_ai_consumer_vault_disabled.env | nindent 10 }}
          {{ if .Values.agentic_ai_settings.RABBITMQ_TLS_ENABLED }}
          - name: RABBITMQ_TLS_PASSWORD
            valueFrom:
              secretKeyRef:
                name: rabbitmq-password
                key: rabbitmq-password
          {{ end }}
          {{ if .Values.agentic_ai_settings.KAFKA_SECURITY_ENABLED }}
          - name: KAFKA_SASL_USERNAME
            valueFrom:
              secretKeyRef:
                name: kafka-secrets
                key: kafka-sasl-username
          - name: KAFKA_SASL_PASSWORD
            valueFrom:
              secretKeyRef:
                name: kafka-secrets
                key: kafka-sasl-password
          {{ end }}
          {{- end }}
          volumeMounts:
          {{- if or .Values.agentic_ai_settings.RABBITMQ_TLS_ENABLED .Values.agentic_ai_settings.DATABASE_REQUIRE_SECURE_TRANSPORT }}
            {{ if .Values.agentic_ai_settings.RABBITMQ_TLS_ENABLED }}
            - name: security-certs
              mountPath: {{ .Values.certs_location.certs_default_path }}
              readOnly: true
            {{ end }}
            {{- if .Values.agentic_ai_settings.DATABASE_REQUIRE_SECURE_TRANSPORT }}
            - name: outputdbcerts
              mountPath: {{ .Values.certs_location.database_sslcerts_path }}
            {{ end }}
          {{- end }}
          {{- if .Values.vault_settings.enabled }}
            - name: secrets-store-inline
              mountPath: {{ .Values.secrets_location.secrets_default_path }}
              readOnly: true
          {{- end }}
      volumes:
      {{- if or .Values.agentic_ai_settings.RABBITMQ_TLS_ENABLED .Values.agentic_ai_settings.DATABASE_REQUIRE_SECURE_TRANSPORT }}
        {{ if .Values.agentic_ai_settings.RABBITMQ_TLS_ENABLED }}
        - name: security-certs
          secret:
            secretName: {{ .Values.secrets.security_certs.secretName }}
        {{ end }}
        {{ if .Values.agentic_ai_settings.DATABASE_REQUIRE_SECURE_TRANSPORT }}
        - name: tls-database-certs
          secret:
            secretName: {{ .Values.secrets.database_ssl_secret.secretName }}
            items:
            - key: tls.crt
              path: tls.crt
            - key: tls.key
              path: tls.key
              mode: 0600
            - key: root.crt
              path: root.crt
            defaultMode: 0644
        - name: outputdbcerts
          emptyDir: {}
        {{ end }}
      {{- end }}
      {{- if .Values.vault_settings.enabled }}
        - name: secrets-store-inline
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: {{ .Values.csi.csi_default_name }}
      {{- end }}
      {{- if .Values.agentic_ai_settings.INSTRUMENTATION_ENABLED }}
        {{- if .Values.agentic_ai_settings.EXPORTER_TYPE "azure" }}
        - name: APPLICATION_INSIGHTS_CONNECTION_STRING
          valueFrom:
            secretKeyRef:
              name: {{ .Values.secrets.telemetry_secrets.secretName }}
              key: application-insights-connection-string
        {{- end }}
      {{- end }}

      {{- with .Values.agentic_ai_consumer.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.agentic_ai_consumer.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.agentic_ai_consumer.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
